server:
  port: 10000
spring:
  thymeleaf:
    cache: false
  # 数据库的配置
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/bplan?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    hikari:
      pool-name: HikariPool
      driver-class-name: com.mysql.jdbc.Driver #com.mysql.cj.jdbc.MysqlDataSource
#      jdbc-url: jdbc:mysql://localhost:3306/bplan?characterEncoding=utf-8&useSSL=false
